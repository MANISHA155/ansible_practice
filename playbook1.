---
  name:create user
  hosts:all
  tasks:
     -name:user creation
      user:
        name:Ravi
        password:intelliqit
        uid:3333
        shell:/bin/bash
        comment:"a regular user"
        home:/tmp/Ravi

       

    
